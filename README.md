# Project Structure
This project is structured as a monorepo to support the potential addition of a backend (content management system) for the head chef to easily update menus in the future. Currently, the repository includes a single folder for the frontend application.
